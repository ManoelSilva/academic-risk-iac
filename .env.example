# Grafana admin credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Model API port (default: 5000)
# On macOS, port 5000 is used by AirPlay Receiver — set to 5050 or another free port
MODEL_API_PORT=5000
